import { DocsTopic } from '../docs-data';

export const phpTopics: DocsTopic[] = [
    {
        id: 'introduction', title: 'Introduction', description: 'Apa itu PHP dan dasar-dasar syntax.', language: 'php',
        content: `<h2>Apa itu PHP?</h2><p>PHP (PHP: Hypertext Preprocessor) adalah bahasa server-side untuk web development.</p><h3>Fitur</h3><ul><li>Berjalan di server (bukan browser)</li><li>Bisa disisipkan dalam HTML</li><li>Support database (MySQL, PostgreSQL)</li><li>Digunakan oleh WordPress, Laravel</li></ul>`,
        codeExample: `<?php\necho "<h1>Hello from PHP!</h1>";\n\n$name = "Spear";\n$version = 2.0;\necho "<p>Welcome to $name v$version</p>";\necho "<p>Today is: " . date("l, d M Y") . "</p>";\n?>`,
    },
    {
        id: 'variables', title: 'Variables & Types', description: 'Variabel, tipe data, dan operator.', language: 'php',
        content: `<h2>PHP Variables</h2><p>Variabel dimulai dengan <code>$</code>. PHP adalah loosely typed.</p><h3>Tipe Data</h3><ul><li>String, Integer, Float, Boolean</li><li>Array, Object, NULL</li></ul><h3>Type Casting</h3><p><code>(int)</code>, <code>(string)</code>, <code>(float)</code>, <code>(bool)</code>, <code>(array)</code></p>`,
        codeExample: `<?php\n$name = "PHP Developer";\necho "Name: $name\\n";\n\n$age = 25;\necho "Age: $age\\n";\n\n$price = 99.99;\necho "Price: $$price\\n";\n\n$isActive = true;\necho "Active: " . ($isActive ? "Yes" : "No") . "\\n";\n\n$skills = ["HTML", "CSS", "JS", "PHP"];\necho "Skills: " . implode(", ", $skills) . "\\n";\n\n$user = ["name" => "Ali", "level" => 5];\necho "User: " . $user["name"] . " (Level " . $user["level"] . ")";\n?>`,
    },
    {
        id: 'strings', title: 'Strings', description: 'Manipulasi teks di PHP.', language: 'php',
        content: `<h2>PHP Strings</h2><h3>Quotes</h3><ul><li>Double quotes: variabel di-parse (<code>"Hello $name"</code>)</li><li>Single quotes: literal (<code>'Hello $name'</code> → $name as text)</li></ul><h3>Functions</h3><ul><li><code>strlen()</code>, <code>str_word_count()</code></li><li><code>strtoupper()</code>, <code>strtolower()</code>, <code>ucfirst()</code></li><li><code>strpos()</code>, <code>substr()</code></li><li><code>str_replace()</code>, <code>trim()</code></li><li><code>explode()</code>, <code>implode()</code></li><li><code>sprintf()</code></li></ul>`,
        codeExample: `<?php\n$str = "Hello, PHP World!";\n\necho "strlen: " . strlen($str) . "\\n";\necho "strtoupper: " . strtoupper($str) . "\\n";\necho "strtolower: " . strtolower($str) . "\\n";\necho "substr(7,3): " . substr($str, 7, 3) . "\\n";\necho "strpos('PHP'): " . strpos($str, "PHP") . "\\n";\necho "str_replace: " . str_replace("PHP", "Laravel", $str) . "\\n";\n\n$csv = "HTML,CSS,JS,PHP";\n$arr = explode(",", $csv);\necho "explode: " . print_r($arr, true) . "\\n";\necho "implode: " . implode(" | ", $arr) . "\\n";\n\necho sprintf("Name: %s, Score: %d%%", "Ali", 95);\n?>`,
    },
    {
        id: 'numbers', title: 'Numbers & Math', description: 'Perhitungan dan fungsi matematika.', language: 'php',
        content: `<h2>PHP Numbers</h2><h3>Functions</h3><ul><li><code>abs()</code>, <code>round()</code>, <code>ceil()</code>, <code>floor()</code></li><li><code>max()</code>, <code>min()</code></li><li><code>rand()</code>, <code>mt_rand()</code></li><li><code>sqrt()</code>, <code>pow()</code></li><li><code>number_format()</code></li><li><code>intval()</code>, <code>floatval()</code></li><li><code>is_numeric()</code>, <code>is_int()</code></li></ul>`,
        codeExample: `<?php\necho "abs(-15): " . abs(-15) . "\\n";\necho "round(4.567, 2): " . round(4.567, 2) . "\\n";\necho "ceil(4.1): " . ceil(4.1) . "\\n";\necho "floor(4.9): " . floor(4.9) . "\\n";\necho "max(3,7,2,9): " . max(3,7,2,9) . "\\n";\necho "rand(1,100): " . rand(1, 100) . "\\n";\necho "sqrt(144): " . sqrt(144) . "\\n";\necho "pow(2,8): " . pow(2, 8) . "\\n";\necho "number_format(1234567.89): " . number_format(1234567.89, 2, ",", ".") . "\\n";\necho "is_numeric('42'): " . (is_numeric("42") ? "true" : "false") . "\\n";\necho "PI: " . M_PI;\n?>`,
    },
    {
        id: 'operators', title: 'Operators', description: 'Operator aritmatika, perbandingan, logika.', language: 'php',
        content: `<h2>PHP Operators</h2><h3>Aritmatika</h3><ul><li><code>+</code> <code>-</code> <code>*</code> <code>/</code> <code>%</code> <code>**</code></li></ul><h3>Perbandingan</h3><ul><li><code>==</code> vs <code>===</code> (strict)</li><li><code>!=</code> vs <code>!==</code></li><li><code><=></code> — spaceship operator (PHP 7)</li></ul><h3>Logika</h3><ul><li><code>&&</code>, <code>||</code>, <code>!</code>, <code>and</code>, <code>or</code></li></ul><h3>String</h3><ul><li><code>.</code> — concatenation</li><li><code>.=</code> — concatenation assignment</li></ul><h3>Null</h3><ul><li><code>??</code> — null coalescing</li><li><code>??=</code> — null coalescing assignment</li></ul>`,
        codeExample: `<?php\n// Perbandingan\necho "== : '5' == 5 → " . var_export("5" == 5, true) . "\\n";\necho "=== : '5' === 5 → " . var_export("5" === 5, true) . "\\n\\n";\n\n// Spaceship\necho "1 <=> 2 → " . (1 <=> 2) . "\\n";\necho "2 <=> 2 → " . (2 <=> 2) . "\\n";\necho "3 <=> 2 → " . (3 <=> 2) . "\\n\\n";\n\n// Concatenation\n$greeting = "Hello" . ", " . "World!";\necho $greeting . "\\n\\n";\n\n// Null coalescing\n$name = null;\necho "name ?? 'Guest' → " . ($name ?? "Guest") . "\\n";\n\n$name ??= "Default";\necho "After ??= : $name";\n?>`,
    },
    {
        id: 'control-flow', title: 'Control Flow', description: 'If/else, switch, match di PHP.', language: 'php',
        content: `<h2>PHP Control Flow</h2><ul><li><code>if / elseif / else</code></li><li><code>switch / case</code></li><li><code>match</code> (PHP 8) — strict comparison, return value</li></ul>`,
        codeExample: `<?php\n$score = 85;\nif ($score >= 90) $grade = "A";\nelseif ($score >= 80) $grade = "B";\nelseif ($score >= 70) $grade = "C";\nelse $grade = "D";\necho "Score: $score → Grade: $grade\\n\\n";\n\n// Match (PHP 8)\n$status = match(true) {\n    $score >= 90 => "Excellent",\n    $score >= 80 => "Good",\n    $score >= 70 => "Average",\n    default => "Below Average"\n};\necho "Status: $status\\n\\n";\n\n// Ternary\n$result = $score >= 75 ? "Pass ✅" : "Fail ❌";\necho "Result: $result";\n?>`,
    },
    {
        id: 'loops', title: 'Loops', description: 'For, while, foreach, do-while.', language: 'php',
        content: `<h2>PHP Loops</h2><ul><li><code>for</code> — loop klasik</li><li><code>while</code> — loop dengan kondisi</li><li><code>do...while</code> — minimal 1x</li><li><code>foreach</code> — iterasi array (cara PHP)</li><li><code>break</code> / <code>continue</code></li></ul>`,
        codeExample: `<?php\n// For\necho "For: ";\nfor ($i = 1; $i <= 5; $i++) echo "$i ";\necho "\\n\\n";\n\n// Foreach (indexed)\n$langs = ["HTML", "CSS", "JS", "PHP"];\necho "Foreach: ";\nforeach ($langs as $lang) echo "$lang ";\necho "\\n\\n";\n\n// Foreach (associative)\n$user = ["name" => "Ali", "age" => 25, "level" => 5];\necho "Key-Value:\\n";\nforeach ($user as $key => $value) {\n    echo "  $key: $value\\n";\n}\n\n// While + break\necho "\\nWhile: ";\n$n = 0;\nwhile (true) { $n++; if ($n > 5) break; echo "$n "; }\n?>`,
    },
    {
        id: 'functions', title: 'Functions', description: 'Membuat dan menggunakan fungsi.', language: 'php',
        content: `<h2>PHP Functions</h2><h3>Fitur</h3><ul><li>Default parameters</li><li>Type hints (PHP 7+)</li><li>Return type declaration</li><li>Named arguments (PHP 8)</li><li>Variadic (...$args)</li></ul>`,
        codeExample: `<?php\nfunction greet(string $name): string {\n    return "Hello, $name!";\n}\necho greet("World") . "\\n";\n\n// Default + type hints\nfunction area(float $w, float $h = 10): float {\n    return $w * $h;\n}\necho "Area: " . area(5) . "\\n";\necho "Area: " . area(5, 3) . "\\n";\n\n// Variadic\nfunction sum(int ...$nums): int {\n    return array_sum($nums);\n}\necho "Sum: " . sum(1,2,3,4,5) . "\\n";\n\n// Return array\nfunction stats(array $nums): array {\n    return ['sum' => array_sum($nums), 'avg' => array_sum($nums)/count($nums), 'max' => max($nums)];\n}\n$s = stats([10,20,30,40,50]);\nforeach ($s as $k => $v) echo "$k: $v\\n";\n?>`,
    },
    {
        id: 'arrays', title: 'Arrays', description: 'Indexed, associative, dan array functions.', language: 'php',
        content: `<h2>PHP Arrays</h2><h3>Jenis</h3><ul><li><strong>Indexed</strong> — index numerik</li><li><strong>Associative</strong> — key-value</li><li><strong>Multidimensional</strong> — nested</li></ul><h3>Functions</h3><ul><li><code>sort()</code>, <code>rsort()</code>, <code>asort()</code>, <code>ksort()</code></li><li><code>array_push()</code>, <code>array_pop()</code></li><li><code>array_merge()</code>, <code>array_slice()</code></li><li><code>array_filter()</code>, <code>array_map()</code></li><li><code>in_array()</code>, <code>array_search()</code></li><li><code>count()</code>, <code>array_unique()</code></li></ul>`,
        codeExample: `<?php\n$fruits = ["Apple", "Banana", "Cherry"];\necho "Fruits: " . implode(", ", $fruits) . "\\n";\n\n$student = ["name" => "Budi", "age" => 17, "grade" => "A"];\necho "Student: " . $student["name"] . " (Grade " . $student["grade"] . ")\\n\\n";\n\n// Array functions\n$nums = [3,1,4,1,5,9,2,6];\nsort($nums);\necho "Sorted: " . implode(",", $nums) . "\\n";\n\n$filtered = array_filter($nums, fn($n) => $n > 3);\necho "Filtered (>3): " . implode(",", $filtered) . "\\n";\n\n$squared = array_map(fn($n) => $n**2, $nums);\necho "Squared: " . implode(",", $squared) . "\\n";\n\necho "Unique: " . implode(",", array_unique($nums));\n?>`,
    },
    {
        id: 'superglobals', title: 'Superglobals', description: '$_GET, $_POST, $_SESSION, $_SERVER.', language: 'php',
        content: `<h2>PHP Superglobals</h2><p>Variabel bawaan PHP yang bisa diakses dari mana saja.</p><ul><li><code>$_GET</code> — parameter URL</li><li><code>$_POST</code> — data form POST</li><li><code>$_REQUEST</code> — gabungan GET/POST</li><li><code>$_SERVER</code> — info server dan request</li><li><code>$_SESSION</code> — data session</li><li><code>$_COOKIE</code> — cookies</li><li><code>$_FILES</code> — file upload</li><li><code>$_ENV</code> — environment variables</li><li><code>$GLOBALS</code> — semua variabel global</li></ul>`,
        codeExample: `<?php\n// $_SERVER info\necho "Server Info:\\n";\necho "PHP_SELF: " . ($_SERVER['PHP_SELF'] ?? '/index.php') . "\\n";\necho "SERVER_NAME: " . ($_SERVER['SERVER_NAME'] ?? 'localhost') . "\\n";\necho "REQUEST_METHOD: " . ($_SERVER['REQUEST_METHOD'] ?? 'GET') . "\\n";\necho "HTTP_USER_AGENT: " . substr($_SERVER['HTTP_USER_AGENT'] ?? 'Browser', 0, 50) . "\\n\\n";\n\n// Simulating $_GET\n// URL: page.php?name=Ali&age=25\n$name = "Ali"; // $_GET['name']\n$age = 25;     // $_GET['age']\necho "GET params: name=$name, age=$age\\n\\n";\n\n// Form handling\necho "Form Example:\\n";\necho '<form method="post" action="process.php">';\necho '  <input name="email" type="email">';\necho '  <button type="submit">Submit</button>';\necho '</form>';\n?>`,
    },
    {
        id: 'forms', title: 'Forms & Input', description: 'Menangani form data dengan validasi.', language: 'php',
        content: `<h2>PHP Forms</h2><h3>Keamanan</h3><ul><li><code>htmlspecialchars()</code> — cegah XSS</li><li><code>filter_var()</code> — validasi & sanitize</li><li><code>filter_input()</code> — filter dari super global</li></ul><h3>Filter Constants</h3><ul><li><code>FILTER_VALIDATE_EMAIL</code></li><li><code>FILTER_VALIDATE_URL</code></li><li><code>FILTER_VALIDATE_INT</code></li><li><code>FILTER_SANITIZE_STRING</code></li><li><code>FILTER_SANITIZE_EMAIL</code></li></ul>`,
        codeExample: `<?php\n// Simulating form input\n$name = "John";\n$email = "john@example.com";\n$message = "<script>alert('xss')</script>Hello!";\n\n// Sanitize!\n$safeName = htmlspecialchars($name);\n$safeEmail = filter_var($email, FILTER_SANITIZE_EMAIL);\n$safeMsg = htmlspecialchars($message);\n\necho "Name: $safeName\\n";\necho "Email: $safeEmail\\n";\necho "Message: $safeMsg\\n\\n";\n\n// Validate\nif (filter_var($email, FILTER_VALIDATE_EMAIL)) {\n    echo "✅ Email valid\\n";\n} else {\n    echo "❌ Email invalid\\n";\n}\n\n// Validate integer\n$age = "25";\nif (filter_var($age, FILTER_VALIDATE_INT, ['options' => ['min_range' => 1, 'max_range' => 150]])) {\n    echo "✅ Age valid: $age";\n}\n?>`,
    },
    {
        id: 'sessions', title: 'Sessions', description: 'Menyimpan data user lintas halaman.', language: 'php',
        content: `<h2>PHP Sessions</h2><p>Session menyimpan data user di server, persist selama browser terbuka.</p><h3>Cara Kerja</h3><ul><li><code>session_start()</code> — mulai/resume session</li><li><code>$_SESSION['key'] = value</code> — set data</li><li><code>$_SESSION['key']</code> — baca data</li><li><code>session_destroy()</code> — hapus session</li><li><code>unset($_SESSION['key'])</code> — hapus satu key</li></ul>`,
        codeExample: `<?php\nsession_start();\n\n// Set session data\n$_SESSION['username'] = 'Ali';\n$_SESSION['role'] = 'admin';\n$_SESSION['login_time'] = date('H:i:s');\n\n// Read session\necho "Username: " . $_SESSION['username'] . "\\n";\necho "Role: " . $_SESSION['role'] . "\\n";\necho "Login: " . $_SESSION['login_time'] . "\\n\\n";\n\n// Check if logged in\nif (isset($_SESSION['username'])) {\n    echo "✅ User is logged in\\n";\n}\n\n// Session ID\necho "Session ID: " . session_id() . "\\n\\n";\n\n// Destroy (logout)\n// session_destroy();\necho "// session_destroy() → logout user";\n?>`,
    },
    {
        id: 'cookies', title: 'Cookies', description: 'Menyimpan data kecil di browser client.', language: 'php',
        content: `<h2>PHP Cookies</h2><p>Cookie menyimpan data kecil di browser user.</p><h3>Functions</h3><ul><li><code>setcookie(name, value, expire, path)</code></li><li><code>$_COOKIE['name']</code> — baca cookie</li></ul><h3>Perbedaan Session vs Cookie</h3><ul><li><strong>Session</strong>: di server, lebih aman, expire saat browser tutup</li><li><strong>Cookie</strong>: di browser, bisa diset expire, max 4KB</li></ul>`,
        codeExample: `<?php\n// Set cookie (expire 30 hari)\nsetcookie("theme", "dark", time() + (30 * 24 * 60 * 60), "/");\nsetcookie("language", "id", time() + (365 * 24 * 60 * 60), "/");\n\n// Read cookies\n$theme = $_COOKIE['theme'] ?? 'light';\n$lang = $_COOKIE['language'] ?? 'en';\n\necho "Theme: $theme\\n";\necho "Language: $lang\\n\\n";\n\n// Check cookie\nif (isset($_COOKIE['theme'])) {\n    echo "✅ Theme cookie exists\\n";\n}\n\n// Delete cookie (set expire to past)\n// setcookie("theme", "", time() - 3600, "/");\necho "\\n// Delete: setcookie('theme', '', time()-3600, '/')";\n?>`,
    },
    {
        id: 'date-time', title: 'Date & Time', description: 'Tanggal dan waktu di PHP.', language: 'php',
        content: `<h2>PHP Date & Time</h2><h3>Functions</h3><ul><li><code>date(format)</code> — format tanggal</li><li><code>time()</code> — Unix timestamp</li><li><code>mktime()</code> — buat timestamp</li><li><code>strtotime()</code> — parse string ke timestamp</li></ul><h3>Format Characters</h3><ul><li><code>Y</code> year (4 digit), <code>m</code> month, <code>d</code> day</li><li><code>H</code> hour (24), <code>i</code> minute, <code>s</code> second</li><li><code>l</code> full day name, <code>F</code> full month name</li></ul>`,
        codeExample: `<?php\necho "Today: " . date("Y-m-d") . "\\n";\necho "Full: " . date("l, d F Y") . "\\n";\necho "Time: " . date("H:i:s") . "\\n";\necho "Timestamp: " . time() . "\\n\\n";\n\n// strtotime\necho "Tomorrow: " . date("Y-m-d", strtotime("+1 day")) . "\\n";\necho "Next week: " . date("Y-m-d", strtotime("+1 week")) . "\\n";\necho "Last month: " . date("Y-m-d", strtotime("-1 month")) . "\\n\\n";\n\n// DateTime class\n$dt = new DateTime();\necho "DateTime: " . $dt->format("Y-m-d H:i:s") . "\\n";\n\n$dt->modify("+3 days");\necho "+3 days: " . $dt->format("Y-m-d") . "\\n";\n\n$diff = (new DateTime())->diff(new DateTime("2025-01-01"));\necho "Days to 2025: " . $diff->days;\n?>`,
    },
    {
        id: 'file-handling', title: 'File Handling', description: 'Membaca dan menulis file.', language: 'php',
        content: `<h2>PHP File Handling</h2><h3>Functions</h3><ul><li><code>file_get_contents()</code> — baca seluruh file</li><li><code>file_put_contents()</code> — tulis ke file</li><li><code>fopen()</code>, <code>fread()</code>, <code>fwrite()</code>, <code>fclose()</code></li><li><code>file_exists()</code>, <code>is_file()</code>, <code>is_dir()</code></li><li><code>unlink()</code> — hapus file</li><li><code>mkdir()</code>, <code>rmdir()</code></li></ul><h3>Modes</h3><ul><li><code>r</code> read, <code>w</code> write (overwrite), <code>a</code> append</li></ul>`,
        codeExample: `<?php\n// Write file\nfile_put_contents("demo.txt", "Hello, PHP File!\\nLine 2\\nLine 3");\necho "✅ File written\\n\\n";\n\n// Read file\n$content = file_get_contents("demo.txt");\necho "Content:\\n$content\\n\\n";\n\n// Append\nfile_put_contents("demo.txt", "\\nLine 4 (appended)", FILE_APPEND);\n\n// Read lines\n$lines = file("demo.txt", FILE_IGNORE_NEW_LINES);\necho "Lines: " . count($lines) . "\\n";\nforeach ($lines as $i => $line) {\n    echo "  Line $i: $line\\n";\n}\n\n// File info\necho "\\nExists: " . (file_exists("demo.txt") ? "Yes" : "No") . "\\n";\necho "Size: " . filesize("demo.txt") . " bytes\\n";\n\n// Cleanup\nunlink("demo.txt");\necho "\\n🗑️ File deleted";\n?>`,
    },
    {
        id: 'include-require', title: 'Include & Require', description: 'Menyisipkan file PHP lain.', language: 'php',
        content: `<h2>Include & Require</h2><h3>Perbedaan</h3><ul><li><code>include</code> — warning jika file tidak ada (lanjut eksekusi)</li><li><code>require</code> — fatal error jika file tidak ada (stop)</li><li><code>include_once</code> / <code>require_once</code> — hanya include 1x</li></ul><h3>Use Cases</h3><ul><li>Header/footer template</li><li>Database connection</li><li>Helper functions</li><li>Config files</li></ul>`,
        codeExample: `<?php\n// === header.php ===\n// <header><h1>My Site</h1><nav>...</nav></header>\n\n// === footer.php ===\n// <footer>&copy; 2024</footer>\n\n// === db.php ===\n// $pdo = new PDO(...);\n\n// === page.php ===\necho "// Typical PHP page structure:\\n\\n";\n\necho '<?php\\n';\necho 'require_once "config.php";    // config (fatal if missing)\\n';\necho 'require_once "db.php";        // database\\n';\necho 'include "helpers.php";        // optional helpers\\n';\necho 'include "header.php";         // HTML header\\n';\necho '?>\\n\\n';\necho '<main>Page content here</main>\\n\\n';\necho '<?php include "footer.php"; ?>\\n\\n';\n\necho "// require_once prevents loading same file twice\\n";\necho "// Great for class autoloading";\n?>`,
    },
    {
        id: 'error-handling', title: 'Error Handling', description: 'try/catch dan penanganan error.', language: 'php',
        content: `<h2>PHP Error Handling</h2><h3>try/catch</h3><ul><li><code>try</code> — kode yang mungkin error</li><li><code>catch (Exception $e)</code> — tangani error</li><li><code>finally</code> — selalu jalan</li><li><code>throw new Exception()</code> — lempar error</li></ul><h3>Error Levels</h3><ul><li><code>E_ERROR</code> — fatal</li><li><code>E_WARNING</code> — warning</li><li><code>E_NOTICE</code> — notice</li></ul>`,
        codeExample: `<?php\n// try/catch\ntry {\n    $result = 10 / 2;\n    echo "10 / 2 = $result\\n";\n    \n    // This would throw\n    // $result = 10 / 0;\n} catch (DivisionByZeroError $e) {\n    echo "❌ " . $e->getMessage() . "\\n";\n}\n\n// Custom exception\nfunction validateAge(int $age): void {\n    if ($age < 0) throw new InvalidArgumentException("Age cannot be negative");\n    if ($age > 150) throw new RangeException("Age too high");\n    echo "✅ Valid age: $age\\n";\n}\n\ntry {\n    validateAge(25);\n    validateAge(-5);\n} catch (InvalidArgumentException $e) {\n    echo "❌ Invalid: " . $e->getMessage() . "\\n";\n} catch (RangeException $e) {\n    echo "❌ Range: " . $e->getMessage() . "\\n";\n} finally {\n    echo "\\n(finally always runs)";\n}\n?>`,
    },
    {
        id: 'json-php', title: 'JSON', description: 'Encode dan decode JSON di PHP.', language: 'php',
        content: `<h2>PHP JSON</h2><h3>Functions</h3><ul><li><code>json_encode($data)</code> — PHP → JSON string</li><li><code>json_decode($json)</code> — JSON string → PHP object</li><li><code>json_decode($json, true)</code> — JSON → associative array</li></ul>`,
        codeExample: `<?php\n$user = [\n    "name" => "Ali",\n    "age" => 25,\n    "skills" => ["PHP", "MySQL", "JS"],\n    "active" => true\n];\n\n// Encode\n$json = json_encode($user, JSON_PRETTY_PRINT);\necho "PHP Array → JSON:\\n$json\\n\\n";\n\n// Decode to object\n$obj = json_decode($json);\necho "JSON → Object: " . $obj->name . "\\n";\n\n// Decode to array\n$arr = json_decode($json, true);\necho "JSON → Array: " . $arr["name"] . "\\n";\necho "Skills: " . implode(", ", $arr["skills"]) . "\\n\\n";\n\n// API response pattern\n$response = json_encode([\n    "status" => "success",\n    "data" => $user,\n    "timestamp" => date("c")\n], JSON_PRETTY_PRINT);\necho "API Response:\\n$response";\n?>`,
    },
    {
        id: 'oop', title: 'OOP Basics', description: 'Class, objects, inheritance.', language: 'php',
        content: `<h2>PHP OOP</h2><h3>Konsep</h3><ul><li><strong>Class</strong> — blueprint</li><li><strong>Object</strong> — instance dari class</li><li><strong>Properties</strong> — variabel dalam class</li><li><strong>Methods</strong> — fungsi dalam class</li><li><strong>Inheritance</strong> — extends</li><li><strong>Visibility</strong> — public, protected, private</li><li><strong>Constructor</strong> — __construct()</li></ul>`,
        codeExample: `<?php\nclass Character {\n    public function __construct(\n        public string $name,\n        protected int $level = 1,\n        private int $xp = 0\n    ) {}\n    \n    public function gainXP(int $amount): void {\n        $this->xp += $amount;\n        if ($this->xp >= 100) {\n            $this->level++;\n            $this->xp -= 100;\n            echo "🎉 {$this->name} leveled up to {$this->level}!\\n";\n        }\n    }\n    \n    public function getInfo(): string {\n        return "{$this->name} | Lv.{$this->level} | XP:{$this->xp}/100";\n    }\n}\n\nclass Warrior extends Character {\n    public function attack(): string {\n        return "⚔️ {$this->name} attacks!";\n    }\n}\n\n$hero = new Warrior("Arya", 3);\necho $hero->getInfo() . "\\n";\necho $hero->attack() . "\\n";\n$hero->gainXP(120);\necho $hero->getInfo();\n?>`,
    },
    {
        id: 'interfaces-traits', title: 'Interfaces & Traits', description: 'Interfaces dan traits untuk code reuse.', language: 'php',
        content: `<h2>Interfaces & Traits</h2><h3>Interface</h3><p>Kontrak yang harus diimplementasi oleh class. Hanya deklarasi method, tanpa implementasi.</p><h3>Trait</h3><p>Code reuse mechanism — seperti "copy-paste" method ke dalam class. PHP tidak support multiple inheritance, tapi bisa pakai banyak traits.</p>`,
        codeExample: `<?php\ninterface Printable {\n    public function toString(): string;\n}\n\ninterface Saveable {\n    public function save(): bool;\n}\n\ntrait Timestampable {\n    public string $createdAt;\n    public function setTimestamp(): void {\n        $this->createdAt = date("Y-m-d H:i:s");\n    }\n}\n\ntrait Sluggable {\n    public function makeSlug(string $text): string {\n        return strtolower(str_replace(" ", "-", $text));\n    }\n}\n\nclass Article implements Printable, Saveable {\n    use Timestampable, Sluggable;\n    \n    public function __construct(public string $title) {\n        $this->setTimestamp();\n    }\n    \n    public function toString(): string {\n        return "[{$this->createdAt}] {$this->title}";\n    }\n    \n    public function save(): bool { return true; }\n}\n\n$article = new Article("Belajar PHP OOP");\necho $article->toString() . "\\n";\necho "Slug: " . $article->makeSlug($article->title) . "\\n";\necho "Saved: " . ($article->save() ? "Yes" : "No");\n?>`,
    },
    {
        id: 'regex-php', title: 'Regular Expressions', description: 'Pattern matching dengan preg functions.', language: 'php',
        content: `<h2>PHP Regular Expressions</h2><h3>Functions</h3><ul><li><code>preg_match(pattern, subject)</code> — cari 1 match</li><li><code>preg_match_all()</code> — cari semua match</li><li><code>preg_replace()</code> — search & replace</li><li><code>preg_split()</code> — split by pattern</li></ul>`,
        codeExample: `<?php\n$text = "Call 081234567890 or 089876543210";\n\n// Match\nif (preg_match('/08\\d{8,12}/', $text, $match)) {\n    echo "First phone: " . $match[0] . "\\n";\n}\n\n// Match all\npreg_match_all('/08\\d{8,12}/', $text, $matches);\necho "All phones: " . implode(", ", $matches[0]) . "\\n\\n";\n\n// Validate email\n$email = "test@example.com";\nif (preg_match('/^[\\w.-]+@[\\w.-]+\\.\\w{2,}$/', $email)) {\n    echo "✅ Valid email: $email\\n";\n}\n\n// Replace\n$censored = preg_replace('/\\d/', '*', "Phone: 08123456");\necho "Censored: $censored\\n";\n\n// Split\n$words = preg_split('/[\\s,;]+/', "Hello, World; PHP  Regex");\necho "Words: " . implode(" | ", $words);\n?>`,
    },
    {
        id: 'mysql-pdo', title: 'MySQL (PDO)', description: 'Koneksi database dengan PDO.', language: 'php',
        content: `<h2>PHP PDO (MySQL)</h2><p>PDO (PHP Data Objects) adalah cara aman dan modern untuk mengakses database.</p><h3>Keunggulan PDO</h3><ul><li>Prepared statements (anti SQL injection)</li><li>Support banyak database</li><li>Error handling dengan exceptions</li></ul><h3>CRUD</h3><ul><li><strong>C</strong>reate — INSERT</li><li><strong>R</strong>ead — SELECT</li><li><strong>U</strong>pdate — UPDATE</li><li><strong>D</strong>elete — DELETE</li></ul>`,
        codeExample: `<?php\n// Connection\ntry {\n    $pdo = new PDO(\n        "mysql:host=localhost;dbname=myapp;charset=utf8mb4",\n        "root", "password",\n        [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]\n    );\n    echo "✅ Connected!\\n\\n";\n} catch (PDOException $e) {\n    die("❌ Connection failed: " . $e->getMessage());\n}\n\n// CREATE\n$stmt = $pdo->prepare("INSERT INTO users (name, email) VALUES (:name, :email)");\n$stmt->execute(['name' => 'Ali', 'email' => 'ali@mail.com']);\necho "Inserted ID: " . $pdo->lastInsertId() . "\\n";\n\n// READ\n$stmt = $pdo->prepare("SELECT * FROM users WHERE email = ?");\n$stmt->execute(['ali@mail.com']);\n$user = $stmt->fetch(PDO::FETCH_ASSOC);\necho "User: " . $user['name'] . "\\n";\n\n// UPDATE\n$stmt = $pdo->prepare("UPDATE users SET name = ? WHERE id = ?");\n$stmt->execute(['Ali Updated', 1]);\n\n// DELETE\n$stmt = $pdo->prepare("DELETE FROM users WHERE id = ?");\n$stmt->execute([1]);\n\necho "\\n// Always use prepared statements!";\n?>`,
    },
    {
        id: 'arrow-closure', title: 'Arrow & Closure', description: 'Fungsi anonim dan arrow functions.', language: 'php',
        content: `<h2>Closures & Arrow Functions</h2><h3>Closure (Anonymous Function)</h3><p>Fungsi tanpa nama. Gunakan <code>use</code> untuk mengakses variabel luar.</p><h3>Arrow Function (PHP 7.4+)</h3><p>Syntax singkat: <code>fn($x) => $x * 2</code>. Otomatis capture variabel luar (by value).</p>`,
        codeExample: `<?php\n// Closure\n$greet = function(string $name): string {\n    return "Hello, $name!";\n};\necho $greet("World") . "\\n";\n\n// Closure with use\n$prefix = "Mr.";\n$formal = function(string $name) use ($prefix): string {\n    return "$prefix $name";\n};\necho $formal("Ali") . "\\n\\n";\n\n// Arrow function (PHP 7.4+)\n$double = fn($n) => $n * 2;\necho "double(5): " . $double(5) . "\\n";\n\n// Arrow auto-captures outer vars\n$multiplier = 3;\n$multiply = fn($n) => $n * $multiplier;\necho "multiply(7): " . $multiply(7) . "\\n\\n";\n\n// With array functions\n$nums = [1, 2, 3, 4, 5];\n$doubled = array_map(fn($n) => $n * 2, $nums);\n$evens = array_filter($nums, fn($n) => $n % 2 === 0);\n\necho "Doubled: " . implode(",", $doubled) . "\\n";\necho "Evens: " . implode(",", $evens);\n?>`,
    },
    {
        id: 'type-declarations', title: 'Type Declarations', description: 'Type hints dan strict typing di PHP.', language: 'php',
        content: `<h2>PHP Type Declarations</h2><h3>Fitur (PHP 7+)</h3><ul><li>Parameter types</li><li>Return types</li><li>Property types (PHP 7.4+)</li><li>Union types: <code>int|string</code> (PHP 8)</li><li>Nullable: <code>?string</code></li><li><code>declare(strict_types=1)</code></li></ul>`,
        codeExample: `<?php\ndeclare(strict_types=1);\n\n// Parameter + return types\nfunction add(int $a, int $b): int {\n    return $a + $b;\n}\necho "add(3, 5): " . add(3, 5) . "\\n";\n\n// Nullable\nfunction findUser(?int $id): ?string {\n    if ($id === null) return null;\n    return "User #$id";\n}\necho "findUser(1): " . findUser(1) . "\\n";\necho "findUser(null): " . var_export(findUser(null), true) . "\\n\\n";\n\n// Union types (PHP 8)\nfunction format(int|float $num): string {\n    return number_format($num, 2);\n}\necho "format(42): " . format(42) . "\\n";\necho "format(3.14): " . format(3.14) . "\\n\\n";\n\n// Class with typed properties\nclass Product {\n    public function __construct(\n        public readonly string $name,\n        public readonly float $price,\n        public readonly ?string $description = null\n    ) {}\n}\n\n$p = new Product("Widget", 29.99);\necho "Product: $p->name - \\$$p->price";\n?>`,
    },
];
